线段树

```txt
线段树解决的核心问题是：动态区间查询 + 动态区间/单点更新。

适合：
    需要频繁区间查询（和、最大值、最小值、GCD等）
    需要频繁单点/区间修改
    操作在线（边修改边查询）
    数组长度固定（10^5 ~ 10^6级别）
懒标记：
    单点更新 + 区间查询 → 无懒标记（够用）
    区间更新 + 区间查询 → 必须懒标记（否则超时）

void pushup(int u){
    ...
}

void pushdown(int u){
    ...
}

void modify(int u,int l,int r,LL v){
    if(tr[u].l >= l && tr[u].r <= r){
        ...
    }else {
        pushdown(u);
        ...
        pushup(u);
    }
}

Node query(int u,int l,int r){
    if(tr[u].l >= l && tr[u].r <= r){
        return tr[u];
    }else {
        pushdown(u);
        ...
    }
}

void build(int u,int l,int r){
    if(l == r){
        ...
    }else {
        ...
        pushup(u);
    }
}
```
